require 'spec_helper'

describe MustacheForm do
  it 'has a version number' do
    expect(MustacheForm::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
